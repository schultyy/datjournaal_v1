import Constants from '../constants';

const initialState = {
  formErrors: null,
  updating: false,
};

export default function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
